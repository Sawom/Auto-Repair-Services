import React from 'react';
import not from '../../../images/not.gif';
const NotFound = () => {
    return (
        <div>
            <img className='w-100' src={not} alt="" />
        </div>
    );
};

export default NotFound;